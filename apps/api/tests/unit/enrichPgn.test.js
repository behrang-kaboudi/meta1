import { describe, it, expect, beforeAll, beforeEach } from 'vitest';
import { enrichPgn } from '../../shared/chess/enrichPgn';
describe('git test', () => {
  it('works', () => {
    console.log('--- webhook.int.test.ts ---??????????????????????');
    expect(1 + 1).toBe(2);
  });
});
